from random import randint
num = int(input('Digite um número de 1 a 5: '))
sorteado = randint(1, 5)

if num == sorteado:
    print('Você acertou!')
else:
    print('Você errou...O numero certo era {}'.format(sorteado))
