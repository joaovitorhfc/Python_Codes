preçoproduto = float(input('Qual é o valor do produto? R$ '))
novo = preçoproduto - (preçoproduto * 5 / 100)
print ('O valor do produto que era {} na promoção de 5% vai custar {}'.format(preçoproduto,novo))
