vel = float(input('Digite a velocidade em Km/h: '))
limite = 80
precokm = 7
if vel > limite:
    excesso = vel - limite
    multa = excesso * precokm
    print('Você foi multado! A multa é de R$ {:.2f}'.format(multa))
else:
    print('Você estava dentro da velocidade!')
